<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
	xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="
	http://www.springframework.org/schema/beans
	http://www.springframework.org/schema/beans/spring-beans-3.1.xsd
    http://www.springframework.org/schema/security
    http://www.springframework.org/schema/security/spring-security-3.1.xsd
    http://www.springframework.org/schema/context
    http://www.springframework.org/schema/context/spring-context-3.1.xsd">

	<context:property-placeholder location="file:${CPATH2_HOME}/cpath2.properties"/>

	<http access-denied-page="/denied" auto-config='true'>
		<form-login login-page="/login" authentication-failure-url="/login?login_error=t" always-use-default-target="false"/>
		<intercept-url pattern="/logbackStatus" access="ROLE_ADMIN" />
		<intercept-url pattern="/admin" access="ROLE_ADMIN" />
		<intercept-url pattern="/admin/**" access="ROLE_ADMIN" />
		<intercept-url pattern="/metadata/*" access="IS_AUTHENTICATED_ANONYMOUSLY" />
		<intercept-url pattern="/metadata/validations/**" access="ROLE_USER" />
		<intercept-url pattern="/tests" access="ROLE_USER" />
		<intercept-url pattern="/**" access="IS_AUTHENTICATED_ANONYMOUSLY"/>
		<logout/>
	</http>

	<authentication-manager alias="simpleAuth">
		<authentication-provider>
			<user-service>
				<!-- change the default password before starting the web app! -->
				<user name="${cpath2.admin.user}" authorities="ROLE_USER, ROLE_ADMIN" password="${cpath2.admin.password}" />
			</user-service>
		</authentication-provider>
	</authentication-manager>

	<global-method-security secured-annotations="enabled">
		<protect-pointcut expression="execution(* com.springsource..*Service.*(..))"
			access="ROLE_USER" />
	</global-method-security>

</beans:beans>