<?xml version="1.0" encoding="UTF-8"?>

<beans xmlns="http://www.springframework.org/schema/beans"
    xmlns:context="http://www.springframework.org/schema/context"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:ehcache="http://ehcache-spring-annotations.googlecode.com/svn/schema/ehcache-spring"
    xsi:schemaLocation="
        http://www.springframework.org/schema/beans 
        http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
        http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.0.xsd
        http://ehcache-spring-annotations.googlecode.com/svn/schema/ehcache-spring
     	http://ehcache-spring-annotations.googlecode.com/svn/schema/ehcache-spring/ehcache-spring-1.1.xsd">

    <context:annotation-config/>
    
	<context:property-placeholder location="file:${CPATH2_HOME}/cpath.properties"/>

	<!-- simply starting this bean populates the (static) Bridgedb DataSource list from constants and metadataDAO! -->
	<bean id="bioDataTypes" class="cpath.service.BioDataTypes" init-method="init">
		<constructor-arg ref="paxtoolsDAO" />
	</bean>
	
	<bean id="service" class="cpath.service.internal.CPathServiceImpl">
		<!-- let's try one service instance per DAO (if required having other than main one at all...)
		<constructor-arg ref="paxtoolsDAO" index="0"/>
		<constructor-arg ref="proteinsDAO" index="1"/>
		<constructor-arg ref="moleculesDAO" index="2"/>
		<constructor-arg ref="cvFetcher"/>
		-->
		<constructor-arg ref="paxtoolsDAO"/>
		<constructor-arg ref="metadataDAO"/>
	</bean>

	<!-- in addition to Hibernate L2 cache, we can set up a general-purpose java ehcache; shelved for later... -->
	<ehcache:annotation-driven cache-manager="ehCacheManager" default-cache-key-generator="defaultCacheKeyGenerator" />
	<bean id="ehCacheManager" class="org.springframework.cache.ehcache.EhCacheManagerFactoryBean" >
		<property name="configLocation" value="${CPATH2_HOME}/ehcache-service.xml"/>
	</bean>
    <bean id="defaultCacheKeyGenerator" class="com.googlecode.ehcache.annotations.key.StringCacheKeyGenerator" />

</beans>