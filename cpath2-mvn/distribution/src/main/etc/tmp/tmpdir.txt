Use -Djava.io.tmpdir=$CPATH2_HOME/tmp to make it java app tmp dir.

Temporary directory is used by cpath2 for storying (and also - re-using if there exist!) 
downloaded biological ontologies, warehouse and pathway data.